events {} 
stream {
    upstream grpc_servers1 {
        server ep11server1:9876 max_fails=3 fail_timeout=10s;
        server ep11server2:9876 max_fails=3 fail_timeout=10s;
    }

    server {
        listen 9876 ;
	
        proxy_pass grpc_servers1;
	proxy_next_upstream on;
    }
}

